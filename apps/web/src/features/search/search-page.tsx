import { PagePlaceholder } from '@/components/page-placeholder'

export function SearchPage() {
  return (
    <PagePlaceholder
      title="Buscar objetos"
      feature="search"
      todo="Buscador por texto, filtros (categoría, ciudad, fecha) y resultados en tarjetas."
    />
  )
}
